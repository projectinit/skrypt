<% const nofooter = true %>
<html>
<head>
    <% include head %>
    <title>SKRYPT</title>
</head>

<body>
<% include nav %>
<div class="container">
    <div class="row">
    <h1>Welcome home,
        <%= user.username %>!</h1>
</div>
    <div class="row">
            <div class="col-md-6">
        <% for(var i = 0; i < posts.length; i++) { %>
        <% var post = posts[i] %>
        <% include partials/post %>
        <% } %>
        <% if (posts.length === 0) { %>
            <p>No posts</p>
        <% } %>
            </div>
    </div>
</div>

<% include footer %>
</body>

</html>
